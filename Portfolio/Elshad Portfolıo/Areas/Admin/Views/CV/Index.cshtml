@model List<Cv>
@{
    ViewData["Title"] = "Index";
}
<div>
    <!-- ==================================================================================== -->
    <!-- ==================================================================================== -->
    <!-- ==================================================================================== -->

    <div class="mx-3">

        <div style="display:flex; justify-content:space-between; align-items:center">

            <div>
                <h1><strong>Cv</strong></h1>
            </div>
             <div>

           @* <a asp-action="create" asp-controller="cv">
            <button class=" btn btn-info">
            <i class="bi bi-pencil-square"></i>
            Create
            </button>
            </a>*@
            </div>

        </div>

        <div>
            <table class="table">
                <thead>
                    <tr>

                        <th scope="col">CV</th>
                        <th scope="col">Action</th>

                    </tr>
                </thead>
                <tbody>

                    @foreach (var item in Model)
                    {


                        <tr>

                            
                                  <td><a href="~/uploads/Cv/@item.CV">Show CV</a></td>
                            

                            <td>
                                <div>
                                    <div>
                                        <a asp-action="update" asp-controller="cv" asp-route-id="@item.Id">
                                            <button class="btn btn-primary">
                                                <i class="bi bi-pencil-square"></i>

                                            </button>
                                        </a>
                                        @* <a class="mx-1" asp-action="delete" asp-controller="cv" asp-route-id="@item.Id">
                                    <button class="btn btn-danger">
                                    <i class="bi bi-trash"></i>
                                    </button>
                                    </a>*@
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }



                </tbody>
            </table>
        </div>
    </div>
    <!-- ==================================================================================== -->
    <!-- ==================================================================================== -->
    <!-- ==================================================================================== -->





</div>
